{
  "name": "frsc-operations-e-dashboard",
  "version": "1.0.0",
  "description": "An e-dashboard for FRSC operations that allows users to fill out a comprehensive form and export the gathered information into either a DOC or XLS file format with a single click.",
  "main": "index.js",
  "scripts": {
    "build:css": "tailwindcss -i input.css -o styles.css",
    "watch:css": "tailwindcss -i input.css -o styles.css --watch"
  },
  "devDependencies": {
    "autoprefixer": "^10.4.19",
    "postcss": "^8.4.38",
    "tailwindcss": "^3.4.3"
  }
}
